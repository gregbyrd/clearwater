%tr
  %td.name= user.name
  %td.email= user.email
  %td.phone= user.phone
  -if !user.admin?
    %td.slots= user.purchased
  %td.link= link_to "Edit", edit_admin_user_path(user)
  %td.link= link_to "Show", admin_user_path(user)
  %td.link= link_to "Delete", admin_user_path(user), method: :delete
