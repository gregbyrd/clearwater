%h1 Users

= link_to "Add New User", new_admin_user_path
%br
= link_to "Admin Home", admin_path

- if @admin_users
  %h2 
    Admin Users
  = render 'users_table', { users: @admin_users, admin: true }

- @seasons.each do |s|
  %h2
    Season: #{s.year}
  = render 'users_table', { users: s.users, admin: false }



