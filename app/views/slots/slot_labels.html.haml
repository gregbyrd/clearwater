%h2 Slot Assignments for #{@date.print_full}

%p
  Please provide a name for each reserved slot. <br>
  You can:
%ul
  %li Choose 'self' for yourself, 
  %li Choose a guest from your previous guests (if any), or
  %li Provide a new guest name (which will be added to your list).

= form_tag reservations_setlabels_path, method: :patch do
  - @num_slots.times do |index|
    .chooselabel
      = radio_button_tag "label_#{index}", 'self', true
      Self <br>
      = radio_button_tag "label_#{index}", 'select'
      Choose a guest: 
      = select_tag "label_select_#{index}", options_for_select(@user.guests)
      <br>
      = radio_button_tag "label_#{index}", 'new'
      = label_tag "label_new_#{index}", "Guest name: "
      = text_field_tag "label_new_#{index}"

  %p
    = submit_tag "Submit"

